 //            var output = JsonConvert.SerializeObject(mobiles);


                //            Func<Mobile, JObject> objToJson =
                //                o => new JObject(
                //                        new JProperty("ObjectId", o.Name),
                //                        new JProperty("ObjectString", o.Price));

                //            var result = new JObject(new JObject(mobiles.Select(objToJson))).ToString();
                ////            JObject videogameRatings = new JObject(
                ////new JProperty("Halo", 9),
                ////new JProperty("Starcraft", 9),
                ////new JProperty("Call of Duty", 7.5));
                //            //string json = JsonSerializer.SerializeObject(mobiles.ToArray());
                //            //JsonSerializer.Serialize(File.CreateText("path.json"), mobiles.ToArray());
                //            using (JsonTextWriter writer = new JsonTextWriter((File.CreateText("Mob.json"))))
                //            {
                //               // result.WriteTo(writer);
                //                File.WriteAllText("Mob.json", result);
                //            }

                //List<Mobile> goods = new List<Mobile>();
                //driver.Navigate().GoToUrl($"https://rozetka.com.ua/mobile-phones/c80003/");
                //SortByPopularity(driver);
                //ExpandAllPages(driver);
                //GradProductData(driver);
                //return goods;
